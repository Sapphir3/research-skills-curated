# 问题发现记录

针对上游 `330c8e7` / 1.3 的静态观察；未修复，非全面质量验收，也非 OpenCode 特有问题。实际遇到相应路径时据此决定规避、优化或替换。

- 提示不准确：`SKILL.md:347` 推荐 `simplify(..., force=True)` 强制化简；此前核查的 SymPy 1.14.0 实现会丢弃该参数，不能指望它改变结果。`powsimp/logcombine(force=True)` 是不同接口，须单独核对假设与定义域。
- 部署条件：计算需兼容 Python/SymPy，autowrap 等编译路径另需编译器；未发现 OpenCode 专有格式/工具接口障碍。此前实数平方根的有限执行证据不代表所有数学分支验收。
