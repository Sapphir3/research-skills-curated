# 问题发现记录

针对上游 `330c8e7` / 1.2 的静态观察；未修复，非全面质量验收，也非 OpenCode 特有问题。实际遇到相应路径时据此决定规避、优化或替换。

- 数据对应：`scripts/assumption_checks.py:338–360,396` 返回去 NaN 后的异常值位置，不能直接用于原表删行；`184–191,211–214` 的方差排序与组标签顺序可不同，导致图上错配。
- 无效检验与过度结论：同脚本 `45–56,123–144,184–197` 缺失/退化输入可产生 NaN，却被解释为不满足假设；`615–618` 仅凭正态/齐性检验称所有假设满足或自动推荐非参数，不能覆盖独立性和配对/层级设计。
- 效应量：`references/effect_sizes_and_power.md:49` 的手算 Cohen d 将两组方差等权平均，不等 n 时一般不等于按 n−1 加权的合并方差；同文件后面的 bootstrap 示例已有加权公式。
- 统计解释：`SKILL.md:60,251,407` 及效应量参考 `7,444–448` 对 p 值证明效应、BF 等于假设后验赔率、贝叶斯序贯无需停止规则限制的概括不准确；应分别结合零假设条件、先验赔率和具体设计。
- 输出声明：`references/bayesian_statistics.md:319–350` 的示例未显式指定所称95%区间、未计算所称BF；`SKILL.md:173` 的固定三位小数可打印 p=0.000。只报告实际计算的量及精度。
- 部署/绘图：`SKILL.md:100–103` 仅以技能根为 cwd 不足以导入 scripts 内模块；应使用实际 scripts 路径。诊断脚本顶层依赖 NumPy/Pandas/SciPy/Matplotlib/seaborn，comprehensive 入口强制绘图（520–628）；无头路径可按需调用定向函数 plot=False。此前环境未同时满足这些库，本轮未重新探测。
