# Distribution observations

The complete upstream repository includes a second, byte-identical SKILL.md in plugins/nature-academic-search/skills/nature-academic-search/. It is an upstream plugin packaging copy, not an additional independent skill. It is retained to preserve the complete upstream source, installer and plugin capabilities. A recursive host/installer may discover both copies; this distribution has not been installed to verify that behavior. Select the top-level nature-academic-search package. No duplicate-discovery repair or comprehensive runtime validation is claimed.

The upstream runtime requires its documented Python/MCP setup; copying this package alone does not register or start those services. No installer or network search was run.
