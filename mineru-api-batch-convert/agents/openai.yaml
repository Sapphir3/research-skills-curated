interface:
  display_name: "MinerU API Batch Convert"
  short_description: "Convert Zotero PDFs with the MinerU API"
  default_prompt: "Use $mineru-api-batch-convert to convert authorized PDFs or paper folders with MinerU API, skip current outputs, and report elapsed time and image integrity."
